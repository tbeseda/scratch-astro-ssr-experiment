{
  "name": "astro-ssr-experiment",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@tinyhttp/app": "^2.0.5",
    "@tinyhttp/logger": "^1.3.0"
  }
}
